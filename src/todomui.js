import React from "react";
import AppBar from "@mui/material/AppBar";
import Box from "@mui/material/Box";

const T = () => {
  return (
    <Box sx={{ width: 40 }}>
      <AppBar size="small">Atrangi To Do List</AppBar>
    </Box>
  );
};
export default T;
